#version 430

layout(location=1) uniform vec2 transform;

layout(location=12) in vec4 position;

void main()
{
	/*
    if(gl_VertexID == 0)
	gl_Position = vec4(-0.5, -0.5, 0, 1);
    if(gl_VertexID == 1)
	gl_Position = vec4(0.5, -0.5, 0, 1);
    if(gl_VertexID == 2)
	gl_Position = vec4(0.5, 0.5, 0, 1);
    if(gl_VertexID == 3)
	gl_Position = vec4(-0.5, 0.5, 0, 1);
	*/

	gl_Position = position;

	gl_Position.yx +=transform; 
}
